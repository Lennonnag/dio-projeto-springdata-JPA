spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/academia
        username: lennonnag
        password: root
    jpa:
      show-sql: true
      hibernate:
        ddl-auto: update
      properties:
        hibernate.format_sql: true